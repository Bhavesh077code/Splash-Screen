<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="http://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinx-900">
        <div class="form p-10 text-white">
           <form action="/create" method="POST">
                <input class="block w-full  rounded-md outline-non px-5 py-2 bg-zinc-600" name="title"  type="text" placeholder=" Title goes to here" >
                <textarea class="block w-full rounded-md outline-non mt-2 px-5 py-2 resize-none bg-zinc-600"    placeholder="Write your task details"  name="detail"></textarea>
                <input class="mt-2 px-5 py-2 bg-blue-700 rounded-md"  type="submit" value="Create Task">
           </form>
           </div>
        <div class="task flex gap-3 flex-wrap p-10">
            <% if(files.length > 0) { %> 
            <% files.forEach(function (val) { %>
               <div class="task min-w-72  px-3 py-4 rounded-md bg-zinc-600 "> 
                  <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                  <div class="flex w-full justify-between mt-3">
                 <a  class="text-blue-500 inline-block " href="/file/<%= val %>">Read more</a>
                 <a  class="text-red-600" href="/edit/<%= val %>">edit filename</a>
                  <a href="/delete/<%= val %>" class="text-red">Delete</a>

                  </div>
            </div>
            <% }) %>
            <% } else { %>
                <h3 class="text-zinc-700">no task yet</h3>
          <%  } %>
        </div>
</body>
</html>